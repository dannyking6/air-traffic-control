# Air Traffic Control — offline build

Self-contained offline build. Platform SDK integration (ads, analytics, network
services) has been removed; the game runs fully locally.

- Engine: Cocos Creator
- Play: serve the folder with any static web server (e.g. `python3 -m http.server`)
  and open `index.html`.
